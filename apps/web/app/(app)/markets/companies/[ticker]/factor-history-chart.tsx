import type { ReactElement } from "react";

export function FactorHistoryChart(): ReactElement {
  return (
    <div className="h-56 w-full flex items-center justify-center text-xs text-fg-muted">
      Factor history not available.
    </div>
  );
}
